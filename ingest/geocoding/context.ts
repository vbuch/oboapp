/**
 * Geocoding context — combines extracted location data with locality information
 */

export type { GeocodingContext } from "./interfaces";
export type {
  ExtractedLocations,
  StreetSection,
  CadastralProperty,
} from "@/lib/types";
