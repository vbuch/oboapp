# Keep tests analyzable while excluding them from duplication checks.
# Migration scripts are intentionally one-off and structurally similar — also excluded.
sonar.cpd.exclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx,**/*.integration.test.ts,**/__tests__/**,**/__mocks__/**,db/migrate/**