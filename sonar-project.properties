# Keep tests analyzable while excluding them from duplication checks.
sonar.cpd.exclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx,**/*.integration.test.ts,**/__tests__/**,**/__mocks__/**